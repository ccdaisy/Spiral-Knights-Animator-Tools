package xandragon.model.math;

/**
 * Used only for compatibility with the matrix.
 * @author Xan the Dragon
 */
public class Quaternion {
	public float w, x, y, z;
	
	public Quaternion(float _w, float _x, float _y, float _z) {
		w = _w;
		x = _x;
		y = _y;
		z = _z;
	}
}
